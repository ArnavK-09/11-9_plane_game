{
  "name": "11-9_plane_game",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\"",
    "check": "tsc",
    "dev:desktop": "tauri dev",
    "build:desktop": "tauri build"
  },
  "dependencies": {
    "kaplay": "^3001.0.0-beta.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.6.2",
    "typescript": "^5.6.2",
    "vite": "^5.4.8"
  }
}
